# Instead Technical Test Submission

- `tax-form-annotation.json` — example JSON specification
- `SPEC.md` — written documentation
- `src/main/java/com/instead/tax/annotation/` — Java model classes

Note: the example coordinates are illustrative. For production, coordinates should be measured against the exact PDF form revision being rendered.
